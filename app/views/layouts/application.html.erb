<!DOCTYPE html>
<html>
  <head>
    <title>Manabiya</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">
    <div class="container mx-auto">
      <header class="bg-white shadow-md py-4 px-4 mb-4 flex justify-between items-center">
        <h1 class="text-3xl font-bold">
          <%= link_to "Manabiya", root_path, class: "text-rose-500 hover:text-rose-600" %>
        </h1>
        <%= render(partial: "layouts/login") %>
      </header>
      <% if notice || alert %>
        <p class="m-4 p-4 bg-blue-500 text-white shadow-md rounded-md"><%= notice || alert %></p>
      <% end %>
      <div class="px-4">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
